<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/pestphp/pest/preset/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         processIsolation="false"
         stopOnFailure="false"
         cacheDirectory=".phpunit.cache"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/Filament/Resources/*/Pages</directory>
        </exclude>
    </source>

    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>

        <!--
            DB de testing en PostgreSQL — algunos tests dependen de
            features específicas de Postgres (CTE recursivo, JSONB).
            Crear una DB dedicada antes de correr los tests:
                createdb constructora_mayap_test
            O usa la variable DB_DATABASE_TEST para apuntar a otra.
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="constructora_mayap_test"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="5445"/>

        <env name="MAIL_MAILER" value="array"/>

        <!--
            WhatsApp APAGADO en tests. Sin esto los tests leen el .env real
            y, con el contenedor de Evolution arriba, mandan mensajes DE
            VERDAD a los teléfonos que inventa la factory — que son números
            hondureños válidos al azar (fake()->numerify('9#######')).
            Los notificadores ya consultan habilitado() antes de enviar,
            así que apagarlo no rompe nada: simplemente no sale nada.
        -->
        <env name="WHATSAPP_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="REDIS_CLIENT" value="predis"/>

        <env name="SENTRY_LARAVEL_DSN" value=""/>
        <env name="HORIZON_PATH" value="horizon"/>
    </php>
</phpunit>
